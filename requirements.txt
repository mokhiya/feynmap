# FeynMap — Python-тулинг для ПЕРЕСБОРКИ артефактов
# (презентации, демо-ролик, документы, диаграммы). Это НЕ зависимости продукта.
#
# Приложение FeynMap написано на Node.js (backend/ + frontend/) и в рантайме
# использует ТОЛЬКО Anthropic (ключ ANTHROPIC_API_KEY) + локальные эмбеддинги
# Ollama (без ключа). Никаких OpenAI / Google ключей для запуска продукта не нужно.
#
# Установка:  python3 -m pip install -r requirements.txt

# Документы .docx (сценарий ролика, продуктовое описание)
python-docx==1.2.0
# Презентации .pptx (питч-деки)
python-pptx==1.0.2
# Кадры демо-ролика, радар компетенций, композиты
Pillow==12.1.1
numpy==2.4.2
matplotlib==3.10.8

# --- ОПЦИОНАЛЬНО: только если заново генерить ОЗВУЧКУ демо-ролика (OpenAI TTS). ---
# К продукту отношения не имеет; требует OPENAI_API_KEY. Не трогаете озвучку — не ставьте.
# openai==2.24.0

# Внешние СИСТЕМНЫЕ инструменты (НЕ через pip): ffmpeg (видео/аудио);
# draw.io CLI и LibreOffice (предпросмотр .drawio/.pptx/.docx) — опционально.
